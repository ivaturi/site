<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <meta name="Description" content="{{ siteMeta.author.name }}" />
    <title>{{ title }}</title>
    <link rel="stylesheet" href="../styles/books.css" />
    <link rel="alternate" href="/feed.xml" type="application/atom+xml" title="{{ siteMeta.author.name }}" />
  </head>
  <body>
    <div class="container">
      
      <!-- <h1> {{  title  }} </h1> -->
      {% render 'nav.liquid'%}


      
      <!-- <h1> {{  title  }} </h1> -->

      <div class="content">
        {{ content }}
      </div>

      
      <h3 class="category"></h3>
      <ul class="posts">
        {% for post in collections.journal reversed %}
          {% unless post.data.publish contains "no" or post.data.visible contains "no" %}
            <li class="post">
              <span class="post-title">
                <a href="{{ post.url }}">{{ post.data.title }}</a>
              </span>
            </li>
          {% endunless %}
        {% endfor %}
      </ul>
  </body>
</html>
